package shirin.tahmasebi.mscfinalproject.history;

class EmailDetailInteractor {
    EmailDetailListener mListener;

    EmailDetailInteractor(EmailDetailListener listener) {
        mListener = listener;
    }

    interface EmailDetailListener {

    }
}
