package shirin.tahmasebi.mscfinalproject.history;

public class EmailDetailInteractor {
    EmailDetailListener mListener;

    public EmailDetailInteractor(EmailDetailListener listener) {
        mListener = listener;
    }

    public interface EmailDetailListener {

    }
}
